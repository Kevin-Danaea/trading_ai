# ============================================================================
# ARCHIVO DE EJEMPLO DE VARIABLES DE ENTORNO
# ============================================================================
# Copia este archivo a .env y configura tus valores reales
# NUNCA subas el archivo .env a git (está en .gitignore)

# === BINANCE API CONFIGURATION ===
# Obtén estas credenciales desde tu cuenta de Binance:
# https://www.binance.com/en/my/settings/api-management
BINANCE_API_KEY=tu_binance_api_key_aqui
BINANCE_API_SECRET=tu_binance_api_secret_aqui

# === GOOGLE CLOUD CONFIGURATION ===
# Para BigQuery y servicios de Google Cloud
GOOGLE_CLOUD_PROJECT_ID=tu-proyecto-google-cloud
GOOGLE_APPLICATION_CREDENTIALS=ruta/a/tu/archivo/credenciales.json

# === DATABASE CONFIGURATION ===
# URL de conexión a tu base de datos (opcional)
DATABASE_URL=postgresql://user:password@host:port/database

# === TRADING CONFIGURATION ===
# Configuraciones por defecto para trading
DEFAULT_COMMISSION=0.001          # 0.1% de comisión por defecto
DEFAULT_INITIAL_CAPITAL=1000.0    # Capital inicial por defecto en USD

# === API RATE LIMITING ===
# Configuraciones para evitar límites de rate en APIs
API_RATE_LIMIT_DELAY=0.1          # Delay entre llamadas en segundos
MAX_API_RETRIES=3                 # Número máximo de reintentos

# === LOGGING ===
# Nivel de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ============================================================================
# INSTRUCCIONES DE CONFIGURACIÓN
# ============================================================================
#
# 1. BINANCE API:
#    - Crea una cuenta en Binance si no tienes una
#    - Ve a Account > API Management
#    - Crea una nueva API Key con permisos de "Spot & Margin Trading"
#    - Copia la API Key y Secret a las variables correspondientes
#
# 2. GOOGLE CLOUD (OPCIONAL - solo si usas BigQuery):
#    - Crea un proyecto en Google Cloud Platform
#    - Habilita la API de BigQuery
#    - Crea una cuenta de servicio y descarga el archivo JSON
#    - Configura las variables con tu project ID y ruta al archivo
#
# 3. SEGURIDAD:
#    - NUNCA compartas tu archivo .env
#    - NUNCA subas credenciales a repositorios públicos
#    - Usa permisos mínimos necesarios para las API keys
#
# ============================================================================ 